{
  "name": "onzenna-survey-app",
  "version": "1.0.0",
  "private": true,
  "description": "Onzenna customer survey checkout extension",
  "scripts": {
    "dev": "shopify app dev",
    "deploy": "shopify app deploy",
    "generate": "shopify app generate extension"
  },
  "dependencies": {},
  "devDependencies": {
    "@shopify/cli": "^3.91.0"
  }
}
