# Onzenna Survey App
# Checkout UI Extension for customer survey data collection
#
# After registering this app in Shopify Partners (or via `shopify app config push`),
# update the client_id below.

name = "Onzenna Survey"
client_id = ""

[access_scopes]
scopes = "read_customers,write_customers,read_orders"

[auth]
redirect_urls = [ "https://localhost/auth/callback" ]

[webhooks]
api_version = "2024-10"
