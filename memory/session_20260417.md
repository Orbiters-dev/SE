---
name: 2026-04-17 세션 요약
description: CRM 파이프라인 대시보드 대수술 (Import/Generate/Send/Decline 전면 수정), DM 4건, 리플렛 KR, 릴스 번역
type: project
---

## 2026-04-17 세션 요약

### 1. CRM 파이프라인 대시보드 대수술

| 수정 | 내용 |
|------|------|
| Import | EC2 → n8n `import_creators`로 변경 |
| loadCreators | EC2 merge 제거, n8n only |
| Generate Drafts | Parse 노드: regex → indexOf/JSON.parse 방식. 코드블록 스트리핑 추가 |
| max_tokens | 4096 → 8192 (10명 batch 지원) |
| ManyChat 체크 | sent 전환 시 manychat_id 요구 제거 |
| Claude 프롬프트 | "무조건 전원 DM 생성" 절대 규칙 추가 |
| Discovery JP 필터 | 일본어 캡션/해시태그/이름 필터 추가 (해외 84건 제거) |
| 버튼 분리 | draft_ready: 📱IG(복사+프로필열기) / ✅Send / 🗑️Decline |
| IG 링크 | ig.me/m/(DM) → instagram.com/(프로필) 전체 치환 |
| 즉시 반영 | Send/Decline 시 로컬 먼저 변경 → 즉시 UI render → n8n 백그라운드 sync |
| n8n 캐시 | API PUT 후 double-PUT + cache-bust 타임스탬프로 강제 적용 |

### 2. DM 처리

| 인플루언서 | 내용 |
|-----------|------|
| 小林様 (affiliate) | 상품 도착 → 가이드라인 발송 |
| べびもん様 | 유상 ¥50,000 제시 → ¥16,500 역제안 → 최종 거절 |
| もちゃ様 | 타이업 라벨 승인 + 공동투고자 추가 요청 + 내일 18시 투고 |
| 磯野様 | 원본영상 수신 + 보수 계좌입금 + 마무리 DM |

### 3. 근본 원인 분석 (CRM 장애)

| 증상 | 근본 원인 | 해결 |
|------|----------|------|
| Import 0명 | EC2 gk_content_posts 빈 테이블 | n8n 전환 |
| Generate 0개 | 1) 코드블록 파싱 실패 2) 해외 계정 Claude 거부 3) max_tokens 부족 | Parse 재작성 + JP 필터 + 8192 |
| Send 거부 | ManyChat ID 필수 (리치아웃에 부적합) | 체크 제거 |
| 같은 사람 반복 | EC2+n8n 이중 로드 + 테스트 잔존 데이터 | EC2 제거 + cleanup |

### 4. 커밋 이력 (WJ-Test1)

- `f8e6d6b` importFromDiscovery → n8n 기반
- `c859c64` draft_ready 버튼 4개 분리
- `34c0313` Copy/IG/Send/Decline 분리
- `9a8f00d` IG 버튼 = 복사+프로필열기
- `a361a84` loadCreators EC2 제거
- `8e6afdb` markSent renderDraftsList 추가
- `9dbe882` Discovery JP 필터
- `b6fd2b0` IG 프로필 링크 + 즉시 UI 반영

### 5. 오답노트 11건

mistakes_20260417.md에 기록. 핵심:
- dry run 안 하고 push 4번 연속
- 적대감사 안 돌림
- EC2 분리 미완 (관련 코드 전수 확인 안 함)
- 테스트 데이터 cleanup 미흡

### 5b. 후반부 추가 수정 (CRM 대시보드)

| 수정 | 내용 |
|------|------|
| DM Count 오버플로우 | localStorage >30이면 auto-reset. 테스트 중 카운트 과잉 축적 문제 해결 |
| Trash 버튼 | `trashCreator`(confirm dialog) → `declineOne`(즉시 삭제)로 교체 |
| ig.me/m → instagram.com | 프로필 링크 전체 치환 (DM 창 → 프로필 페이지) |
| TikTok/IG 분리 | Import 시 `platform` 필드 감지하여 전송. TikTok 핸들이 IG로 잘못 들어가는 문제 해결 |
| 바이오 이름 사용 | DM 초안에서 핸들 대신 프로필 바이오 첫 줄 이름 사용 (すい様 등) |
| 비공개 계정 스킵 | Parse & Merge Profile 노드에 `isPrivate` 체크 추가 |
| DM 템플릿 통일 | Build Claude Prompt에 세은 제공 템플릿 반영 (구조/서명/톤 고정) |
| 수동 이동 | chiiisan___v → sent, minarai_mamalife → sent, sui.no.nichijo → declined |

### 미완료

- もちゃ様: 내일(4/17) 18시 이후 투고 대기
- 宝珠山様: 4/20 투고 대기
- 村上様: 수정 영상 대기
- 清水様: 해시태그 추가 + 썸네일 대기
- 関根様: 나레이션 추가 영상 대기
- 福島様: 광고코드 + 원본영상 대기
- 小林様: 촬영 후 하서 대기
- 西井様: 촬영 후 하서 대기
- EC2 서버 복구 → 이후 CRM을 EC2 API로 재연결
- Batch Size 20+ 지원: max_tokens 추가 증가 또는 자동 분할
